# Round 4 brief — four more "wacky but professional" E&N concepts

Read ~/en-service-website/options/round3/BRIEF.md first. Everything there still applies (the copy, the one-button rule, tech, quality bar, checking, rules), with the changes below.

Ashton saw the round-3 concepts (live list, conveyor, kinetic, 3D box, in ../round3/) and asked for "four more websites, each one in its own wacky direction, but as modern, as cool as those." They don't all need heavy 3D. Each must feel like a different world from the round-3 four and from each other. Look at one or two round-3 pages for the bar, but don't copy their look.

## Changes from round 3
- Write to ~/en-service-website/options/round4/<your-slug>/.
- **Logo (required):** Ashton now has a real logo: lowercase "e&n" knocked out of a black rounded pebble. Vector files are in ~/en-service-website/brand/:
  - en-logo.svg, en-logo-reverse.svg, en-wordmark*.svg
  - en-lockup-horizontal*.svg (the pebble plus "service company")
  - en-badge.svg
  - mockups.html, which shows them in use
  Copy brand/en-logo.svg as your favicon.svg. Put the logo in the header (and ideally the footer), inline as SVG. The knock-out pattern that sits on any background is in ../round3/live-list/index.html (search "en-lockup" / "en-mark"). ../round3/box-3d/logo-paths.js holds the raw path strings, in case you draw the logo on a canvas. Never redraw the logo in a font; wherever the brand appears inside the concept (labels, stamps, receipts, boards), use the real logo.
- **Brand font:** Red Hat Display (Google Fonts) is the closest match to the logo's letters and is now the brand font. Use it as a main face, or pair it deliberately with a characterful display or utility face that suits your concept.
- "Send your list" is still exactly ONE button on the whole page.
- Don't pick a port another agent uses; yours is given in your task.
